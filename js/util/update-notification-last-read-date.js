/**
 *
 * Updates the last read date for all notifications, to sync badge count across
 * devices
 *
 * TODO: This is not yet implemented
 *
 * @module util/update-notification-last-read-date
 */
import $ from 'jquery';
import CONFIG from '../config.js';

export default function updateNotificationLastReadDate (callback) {
    callback = callback || function () {};

   return callback(null);
}
